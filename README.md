## JavaScript Date string methods

So you have a `new Date()`, should I `toString()` or `toDateString()` or `toLocaleString()` or what?

See how they work: https://aaronbeall.github.io/date-string-methods/